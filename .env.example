# 使用 `openssl rand -base64 48` 生成独立随机值后填写，不要提交真实密钥。
JWT_SECRET=

# 可选：忘记管理员密码时临时设置，至少 16 个字符；重置完成后请删除并重启容器。
# GAMENOTE_PASSWORD_RESET_TOKEN=

# 可选：固定到完整版本标签，例如 dingding229/gamenote:1.0.7。
# GAMENOTE_IMAGE=dingding229/gamenote:latest
